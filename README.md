# 📦 Smart Sheet — تطبيق إدارة خطوط إنتاج مصانع الكرتون

إدارة شاملة لعمليات الإنتاج، من المقاسات إلى الأحبار، والصيانة، والمخازن — كلها في تطبيق واحد.

## 📌 نظرة عامة
Smart Sheet هو تطبيق مخصص لمصانع إنتاج الكرتون المموج، مصمم لتبسيط ورقمنة العمليات اليومية التي كانت تُدار سابقًا عبر الأوراق والدفاتر.  
يُمكنه إدارة:

- إدخال وحفظ مقاسات الشيتات مع حسابات دقيقة للإنتاج.
- تسجيل تقارير الأحبار (Ink Reports) مع دعم للصور.
- إدارة سجلات الصيانة للماكينات.
- تتبع واردات المخزن.
- إدارة طاقم العمل (العمال، الإجازات، الإجراءات).
- حساب عدد الشيتات المطلوبة حسب الطول والكمية.
- ضبط تركيب السيريل (Serial Setup) للطباعة.
- النسخ الاحتياطي والاستعادة السحابية عبر Supabase.
- تبديل بين الوضع الليلي والنهاري مع حفظ التفضيلات.

تم تطويره بلغة Flutter مع استخدام Hive للتخزين المحلي وSupabase للنسخ الاحتياطي السحابي، وهو مُصمم ليكون خفيفًا، سريعًا، ويعمل حتى في بيئة المصنع دون اتصال مستمر بالإنترنت.

---

## 🛠️ الميزات الرئيسية

| الميزة | الوصف |
|---|---|
| ✅ حساب مقاسات الشيتات | حساب طول وعرض الشيت تلقائيًا حسب نوع الفلاب (أوڨر فلاب، 1 أو 2 فلاب) مع إضافة 2 مم اختيارية، وعرض نتائج مقاسات خط الإنتاج. |
| ✅ الكاميرا المدمجة | التقاط صور مباشرة أثناء إدخال المقاسات، مع عرضها وحذفها قبل الحفظ. |
| ✅ تقارير الأحبار (Ink Reports) | تسجيل نوع اللون، الكمية باللتر، أبعاد الشيت، وتحميل صور للعينات. |
| ✅ سجلات الصيانة | تتبع عطل الماكينة، تاريخ التبليغ، الإجراء المتخذ، وموقع الإصلاح. |
| ✅ وارد المخزن | تسجيل المواد الواردة بالكمية والوحدة والملاحظات. |
| ✅ إدارة العمال | إضافة/تعديل/حذف العمال، وتسجيل إجراءاتهم (إجازات، غياب، مكافآت...). |
| ✅ الآلة الحاسبة | آلة حاسبة متقدمة تدعم العمليات الرياضية والتعبيرات (x، ÷، %، ±). |
| ✅ ضبط السيريل | حساب أطوال التقطيع (a1, t1, a2, t2) حسب الطول/العرض والسكين. |
| ✅ النسخ الاحتياطي والاستعادة | رفع وتنزيل نسخ احتياطية كاملة من البيانات عبر Supabase Storage. |
| ✅ الوضع الليلي/النهاري | تبديل تلقائي مع حفظ التفضيل في Hive. |
| ✅ جودة الكاميرا قابلة للضبط | اختيار بين منخفضة، متوسطة، أو عالية حسب متطلبات التخزين. |
| ✅ واجهة عربية بالكامل | جميع النصوص، الأزرار، والتعليقات باللغة العربية فقط. |

---

## 📱 لقطات من واجهة المستخدم
(ملاحظة: تُضاف لاحقًا)  
يمكنك إضافة صور للواجهة في مجلد `assets/screenshots/` ثم ربطها هنا.

---

## 🔧 المتطلبات الفنية

| المكون | الإصدار المطلوب |
|---|---|
| Flutter SDK | >=3.7.0 |
| Dart SDK | >=3.0.0 |
| Android Min SDK | 21 (Android 5.0) |
| Android Target SDK | 34 (Android 14) |
| Android Build Tools | 34.0.0+ |
| Android NDK | متوافق مع Flutter |

---

## 📦 التثبيت والتشغيل

1. تحميل المشروع
```bash
git clone https://github.com/yourusername/smart_sheet.git
cd smart_sheet
```

2. تثبيت التبعيات
```bash
flutter pub get
```

3. تهيئة Supabase (اختياري للنسخ الاحتياطي)
- أنشئ حسابًا على https://supabase.io
- أنشئ مشروعًا جديدًا.
- استبدل القيم في `lib/main.dart`:
```dart
const String supabaseUrl = 'https://your-project.supabase.co';
const String supabaseAnonKey = 'your-anon-key';
```

4. تشغيل التطبيق
```bash
flutter run
```

⚠️ ملاحظة مهمة:  
التطبيق يعتمد على أذونات الكاميرا والتخزين. عند أول تشغيل، سيطلب التطبيق هذه الأذونات — تأكد من الموافقة لاستخدام كامل الميزات.

---

## 🔐 التسجيل والدخول (للميزات السحابية)
للنسخ الاحتياطي والاستعادة، تحتاج إلى حساب Supabase.  
يمكن التسجيل من خلال شاشة "إنشاء حساب" داخل التطبيق.  
بعد تسجيل الدخول، ستظهر خيارات النسخ الاحتياطي والاستعادة في القائمة الجانبية.

---

## 🗃️ هيكل الملفات (مختصر)

```
lib/
├── main.dart                  # نقطة الدخول + تهيئة Hive وSupabase
├── providers/
│   ├── auth_provider.dart     # إدارة المصادقة عبر Supabase
│   └── theme_provider.dart    # إدارة الوضع الليلي/النهاري
├── models/
│   ├── worker_model.dart      # نموذج العامل
│   ├── worker_action_model.dart # نموذج الإجراءات (إجازة، غياب...)
│   └── ...                    # نماذج أخرى (ink_report, maintenance, etc.)
├── screens/
│   ├── home_screen.dart       # الشاشة الرئيسية
│   ├── production_line_screen.dart
│   ├── flexo_screen.dart
│   ├── saved_sizes_screen.dart
│   └── ...                    # كل شاشة منفصلة
├── widgets/
│   ├── buttons/
│   │   ├── theme_toggle_button.dart
│   │   └── home_button.dart
│   ├── sheet_size/
│   │   ├── sheet_size_form.dart
│   │   ├── sheet_size_camera.dart
│   │   └── ...                # مكونات مقاسات الشيت
│   ├── workers/
│   │   ├── worker_card.dart
│   │   └── worker_form.dart
│   └── ...                    # جميع المكونات المعاد استخدامها
└── utils/
    └── calculator_screen.dart # آلة حاسبة مخصصة
```

✅ جميع الملفات تحمل أسماء بالعربية في التعليقات والمتغيرات، ولا تحتوي على أي تعليقات أو مصطلحات إنجليزية غير ضرورية.

---

## 🛡️ التوقيع والنشر (للمطورين)
لنشر التطبيق على Google Play Store:

- أنشئ مفتاح توقيع (Upload Keystore) كما هو موضح في وثائق Flutter.
- أنشئ ملف `key.properties` في مجلد `android/`:

```properties
storePassword=your_password
keyPassword=your_password
keyAlias=upload
storeFile=/path/to/upload-keystore.jks
```

- قم ببناء ملف App Bundle (الأفضل للنشر):
```bash
flutter build appbundle
```

- ارفع الملف الناتج (`app.aab`) من خلال Google Play Console.

✅ تم تكوين `build.gradle.kts` لدعم التوقيع التلقائي عند توفر `key.properties`.

---

## 💡 ملاحظات للمطورين
- تم استخدام Hive كمخزن محلي سريع وموثوق، مع تجنب أي تأثير مباشر له على مظهر التطبيق (كما طلبت).
- لا يتم استخدام Firebase — فقط Supabase للنسخ الاحتياطي.
- جميع الأسماء تنتهي بـ `model` (مثل `worker_model`, `ink_report_model`).
- جميع الملفات المولدة (مثل `.g.dart`) تم تضمينها في المشروع وتحديثها تلقائيًا.
- لا توجد أي مكتبات غير ضرورية — كل مكتبة لها وظيفة محددة في سياق التصنيع.

---

## 📞 دعم وتطوير مستقبلي
إذا كنت بحاجة إلى:

- إضافة ميزة جديدة (مثل: طباعة PDF تلقائي، QR Code للشيتات، تكامل مع ERP).
- تحسين الأداء في أجهزة قديمة.
- دعم لغات إضافية (حتى لو كانت العربية فقط حاليًا).

فلا تتردد في التواصل. التطبيق مصمم ليكون قابلًا للتوسع، ويُمكن تطويره بسهولة مع الحفاظ على هيكله النظيف.

---

✅ تم التصميم والتطوير وفقًا لاحتياجات صناعة الكرتون — لا تطبيقات عامة، بل حلول صناعية عملية.  
تم التأكد من أن كل سطر كود يخدم الغرض العملي، ولا يحتوي على زخارف غير ضرورية.

---

مُعدّ من قبل: carton_pro  
النسخة: 0.1.0+1  
تاريخ الإصدار: 21 نوفمبر 2025